{
  "name": "react-uploady-demo",
  "private": true,
  "version": "0.1.0",
  "description": "A real file-upload demo: react-uploady frontend + a Node/Express backend that stores and serves files.",
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,green \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "build": "npm run build --prefix client",
    "build:embed": "npm run build:embed --prefix client",
    "lint": "npm run lint --prefix client",
    "install:all": "npm install --prefix client && npm install --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
